portal.txt seem to require continuous ids

books.txt seem to require continuous local BOOK_*

A proper parser should get written which understands the native format. Currently code after comment delimeters (--) gets handled, but it obviously shouldnt

itemtype.txt: mgr.typeCreate needs to have continuous ids
